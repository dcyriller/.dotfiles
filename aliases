# alias GNU awk to overwrite (if present) old awk
if command -v gawk >/dev/null; then
  alias awk=gawk
fi

# Pretty print the path
alias path='echo $PATH | tr -s ":" "\n"'

# Fix conflicts with editor
alias grc='vim $(git diff --name-only | uniq)'

alias g=git
